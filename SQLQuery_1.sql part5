CREATE TABLE lakshmi.employee_info(

    employee_id INT PRIMARY KEY,
    first_name VARCHAR(50),
    last_name VARCHAR(50),
    job_title VARCHAR(50),
    salary DECIMAL(10,2)
    )

select * from lakshmi.employee_info;
